# URL da API no desenvolvimento (npm run dev) - Opcao A (mesma origem).
# As chamadas vao para "/api" e o proxy do Vite (vite.config.js) encaminha
# para o backend Quarkus em http://localhost:8080.
VITE_API_BASE_URL=/api
