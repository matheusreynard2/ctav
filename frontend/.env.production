# URL da API em producao (usada no `npm run build`) - Opcao A (mesma origem).
# As chamadas vao para "/api" e o Nginx encaminha para o backend Quarkus.
# Configuracao do proxy: deploy/nginx.conf
# Sem esse proxy, /api retorna o index.html (HTML) -> erro "formato inesperado".
VITE_API_BASE_URL=/api
